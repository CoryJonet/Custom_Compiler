int x;
int y;
int z;
bool flag;
bool done;

// Function to test arithmetic
int test_arithmetic() {

    int c;
    c = 10;    
    cout << "********Testing arithmetic********\n";

    // Test ASSIGN
    cout << "****Testing Assign(s)****\n";
    x = 10;
    y = 8;
    z = 4;
    z = -y;
    flag = false;

    x = x + z;

    if (!flag) {

       // Test POST INCREMENT/DECREMENT
       cout << "****Testing post increment/decrement****\n";    
       x++;
       z--;

       // Test arithmetic
       cout << "****Testing plus, minus, multiplication, division****\n";
       x = y + z;
       x = z - z;
       x = y * z;
       x = y / z;

    }

}

// Tests if/else if/else/while
void test_conditionals() {

     int i;
     i = 0;
     
     // Test if
     if(true) {

     	 // Test while in if
         while (i < 10) {
    	   i++;
	 }

     }

    // Test if with else
    if (i < 10) {
     	int j;
	j = 85;
    }

    else {
     
	int k;

	k = 1;

	while (i < 15) {

	      if ((k / 10) > 1) {
	      	 int l;
		 l = l + 6;
	      }
	      i++;
	}
    }
}

void test_function_params(int a) {

     cout << a;

}

int test_function_total(int b) {

}

void main() {
  
     int d;
     int counter;
     bool flag2;
     d = 1;
     flag2 = true;

     d = test_arithmetic();

     test_conditionals();

     test_function_params(d);
 
     d = test_function_total(d);

         
     if (d == 1) {
     
	d = 2;

     }
     
     counter = 0;
     while (counter < 10) {
     	   counter++;
     }   	  

     cout << flag2;
     cout << "End of program!\n";
}

